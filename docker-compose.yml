x-api-common: &api-common
  image: ghcr.io/bgluis/rinha-api-2026:latest
  volumes:
    - shared_sockets:/tmp/sockets
  networks:
    - rinha-net
  security_opt:
    - seccomp:unconfined
  ulimits:
    nofile:
      soft: 65535
      hard: 65535
  deploy:
    resources:
      limits:
        cpus: '0.30'
        memory: '100M'

services:
  lb:
    image: ghcr.io/bgluis/rinha-lb-2026:latest
    environment:
      - PORT=9999
      - UPSTREAMS=/tmp/sockets/api1.sock,/tmp/sockets/api2.sock,/tmp/sockets/api3.sock,/tmp/sockets/api4.sock
    volumes:
      - shared_sockets:/tmp/sockets
    ports:
      - "9999:9999"
    depends_on:
      - api1
      - api2
      - api3
      - api4
    networks:
      - rinha-net
    security_opt:
      - seccomp:unconfined
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
      memlock:
        soft: -1
        hard: -1
    deploy:
      resources:
        limits:
          cpus: '0.20'
          memory: '100M'

  api1:
    <<: *api-common
    environment:
      - SOCKET_PATH=/tmp/sockets/api1.sock
      - GOGC=off
      - GC_MODE=off
      - GOMEMLIMIT=90MiB
      - GOMAXPROCS=1

  api2:
    <<: *api-common
    environment:
      - SOCKET_PATH=/tmp/sockets/api2.sock
      - GOGC=off
      - GC_MODE=off
      - GOMEMLIMIT=90MiB
      - GOMAXPROCS=1

  api3:
    <<: *api-common
    environment:
      - SOCKET_PATH=/tmp/sockets/api3.sock
      - GOGC=off
      - GC_MODE=off
      - GOMEMLIMIT=90MiB
      - GOMAXPROCS=1

  api4:
    <<: *api-common
    environment:
      - SOCKET_PATH=/tmp/sockets/api4.sock
      - GOGC=off
      - GC_MODE=off
      - GOMEMLIMIT=90MiB
      - GOMAXPROCS=1

  warmup:
    image: curlimages/curl:8.8.0
    platform: linux/amd64
    entrypoint: ["/bin/sh", "/warmup.sh"]
    environment:
      - BASE_URL=http://lb:9999
      - READY_RETRIES=240
      - READY_SLEEP=0.25
      - WARMUP_ROUNDS=48
    volumes:
      - ./scripts/warmup.sh:/warmup.sh:ro
    depends_on:
      - lb
    networks:
      - rinha-net
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: "50M"
    restart: "no"

volumes:
  shared_sockets:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs

networks:
  rinha-net:
    driver: bridge
