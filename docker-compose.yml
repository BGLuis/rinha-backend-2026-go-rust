x-api-common: &api-common
  build: .
  volumes:
    - shared_sockets:/tmp/sockets
  networks:
    - rinha-net
  security_opt:
    - seccomp:unconfined
  ulimits:
    nofile:
      soft: 65535
      hard: 65535
  deploy:
    resources:
      limits:
        cpus: '0.34'
        memory: '75M'

services:
  lb:
    build:
      context: .
      dockerfile: Dockerfile.lb
    environment:
      - PORT=9999
      - UPSTREAMS=/tmp/sockets/api1.sock,/tmp/sockets/api2.sock,/tmp/sockets/api3.sock,/tmp/sockets/api4.sock
    volumes:
      - shared_sockets:/tmp/sockets
    ports:
      - "9999:9999"
    depends_on:
      - api1
      - api2
      - api3
      - api4
    networks:
      - rinha-net
    security_opt:
      - seccomp:unconfined
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
      memlock:
        soft: -1
        hard: -1
    deploy:
      resources:
        limits:
          cpus: '0.12'
          memory: '40M'
  api1:
    <<: *api-common
    environment:
      - SOCKET_PATH=/tmp/sockets/api1.sock
      - GOGC=off
      - GC_MODE=off
      - GOMEMLIMIT=70MiB
      - GOMAXPROCS=1

  api2:
    <<: *api-common
    environment:
      - SOCKET_PATH=/tmp/sockets/api2.sock
      - GOGC=off
      - GC_MODE=off
      - GOMEMLIMIT=70MiB
      - GOMAXPROCS=1

  api3:
    <<: *api-common
    environment:
      - SOCKET_PATH=/tmp/sockets/api3.sock
      - GOGC=off
      - GC_MODE=off
      - GOMEMLIMIT=70MiB
      - GOMAXPROCS=1

  api4:
    <<: *api-common
    environment:
      - SOCKET_PATH=/tmp/sockets/api4.sock
      - GOGC=off
      - GC_MODE=off
      - GOMEMLIMIT=70MiB
      - GOMAXPROCS=1

  warmup:
    image: curlimages/curl:8.8.0
    platform: linux/amd64
    entrypoint: ["/bin/sh", "/warmup.sh"]
    environment:
      - BASE_URL=http://lb:9999
      - READY_RETRIES=240
      - READY_SLEEP=0.25
      - WARMUP_ROUNDS=48
    volumes:
      - ./scripts/warmup.sh:/warmup.sh:ro
    depends_on:
      - lb
    networks:
      - rinha-net
    deploy:
      resources:
        limits:
          cpus: "0.04"
          memory: "10M"
    restart: "no"

volumes:
  shared_sockets:
    driver: local
    driver_opts:
      type: tmpfs
      device: tmpfs

networks:
  rinha-net:
    driver: bridge
