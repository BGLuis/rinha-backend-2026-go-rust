[package]
name = "rust_lb"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
mio = { version = "0.8", features = ["os-poll", "net"] }

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
overflow-checks = false
opt-level = 3
